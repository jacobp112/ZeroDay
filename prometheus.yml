global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'celery'
    static_configs:
      - targets: ['api:8000'] # Assuming we expose metrics on API or use a separate exporter
# Real production needs celery-exporter container sidecar
